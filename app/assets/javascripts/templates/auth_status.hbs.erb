{{#if facebookAuthed}}
  <div class="auth-status-facebook">
    <image class="facebook-icon" src="<%= asset_path('facebook_60.png') %>"></image>
    {{user.canonical_name}} |
    <a id="logout-facebook" {{action logoutClicked "facebook"}}>Logout</a>
  </div>
{{/if}}
